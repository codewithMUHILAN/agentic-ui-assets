# Agentic UI Component Assets

This repository hosts the static code assets (ZIP files) for Agentic UI components, served globally via jsDelivr CDN.

### How to Deploy
1. Create a public repository named `agentic-ui-assets` on GitHub under your account `codewithMUHILAN`.
2. Push this local directory to GitHub using the commands in the terminal.
